apiVersion: v1
kind: Service
metadata:
  name: opencode
  namespace: gross-view
  labels:
    app.kubernetes.io/name: opencode
    app.kubernetes.io/part-of: gross-view
spec:
  selector:
    app.kubernetes.io/name: opencode
  ports:
    - name: http
      port: 4096
      targetPort: http
