# Kubernetes Secret for gross-view stack.
# NOTE: Values below are placeholders. Fill them before applying (kubectl apply -k).
# Do not commit real secrets to git. For production, manage via ExternalSecrets.
apiVersion: v1
kind: Secret
metadata:
  name: gross-view-secrets
  namespace: gross-view
  labels:
    app.kubernetes.io/part-of: gross-view
type: Opaque
stringData:
  # Postgres superuser (matches docker-c  postgres_user: pg_dba_user
  postgres_password: "Zf3tXQ%Yc^7K*T"
  postgres_db: postgres

  # Keycloak database (KC_*)
  kc_db_user: kc_dba_user
  kc_db_password: "72mR@#3R@Tws9N"
  kc_db_name: keycloak

  # Gross-view application database (GV_*)
  gv_db_user: gv_dba_user
  gv_db_password: "*Eg3#433@jzHeE"
  gv_db_name: gross_view

  # Keycloak admin
  keycloak_admin: kc_owner
  keycloak_admin_password: "#k*d@rt434wKpZ"
