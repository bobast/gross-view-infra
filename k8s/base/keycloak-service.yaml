apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: gross-view
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: gross-view
spec:
  selector:
    app.kubernetes.io/name: keycloak
  ports:
    - name: http
      port: 8080
      targetPort: http
