# TLS certificate for gross-view.local used by the Ingress.
# Placeholder — replace with the real PEM data (from the gitignored certs/ dir
# used by docker-compose) before applying, or pre-create the Secret:
#   kubectl -n gross-view create secret tls gross-view-tls \
#     --cert=certs/gross-view.local.crt --key=certs/gross-view.local.key
# If you pre-create it, do NOT list this file in kustomization.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: gross-view-tls
  namespace: gross-view
type: kubernetes.io/tls
data:
  # base64-encoded PEM certificate
  tls.crt: ""
  # base64-encoded PEM private key
  tls.key: ""
